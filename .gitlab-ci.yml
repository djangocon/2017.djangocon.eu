# This file is generated by GitLab CI
before_script:
  - export PROJECT_CONFIG='djangocon_europe'
  - dropdb -U postgres $PROJECT_CONFIG --if-exists
  - createdb -U postgres $PROJECT_CONFIG

ci:
  script:
    - bash runtests.sh
  variables:
    SECRET_KEY: 'IOIO'
    ALLOWED_HOSTS: '["*"]'
    CACHE_URL: 'dummycache://'
    DATABASE_URL: 'SQLITE: sqlite://:memory:'
    EMAIL_HOST: 'localhost'
